# roboshop-terraform-module-v2
module to module communication by using output block
when we add provisioner(ansible script file) in an ec2 , then there required private ip address , then cyclic will form
means:
in provisioner, connection required private_ip address so 

